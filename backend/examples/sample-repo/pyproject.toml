[project]
name = "sample-repo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "httpx>=0.27",
    "pydantic[email]>=2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
