# SDK 最小可运行样例（W4）

展示 `register_tool` / `register_agent`。详细说明见本目录 `register_example.py`。

```bash
pip install -e ".[dev]"
python examples/sdk-min/register_example.py
```

期望：打印已注册的 tool 名与 agent 名，exit 0。
